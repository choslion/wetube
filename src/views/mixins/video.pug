mixin videoInfo(eddy)
      div
        h4 
          a(href=`/videos/${eddy.id}`)=eddy.title
        ul
          li #{eddy.id}
          li #{eddy.rating}/5
          li #{eddy.comments} comments.
          li Posted #{eddy.createdAt}
          li #{eddy.views} view.